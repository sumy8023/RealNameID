# RealName Simple 示例发行版

这是用于本机试用和学习的示例文件，不是生产发行版。

- 默认后端地址：`http://127.0.0.1:14848`
- 默认管理后台：`http://127.0.0.1:14850`
- 默认数据库：本机 MySQL 的 `realnameauth_bfftest` 与 `xueji_bfftest`
- 示例账号和口令只用于本机测试，正式环境必须全部替换
- 客户端、Watchdog、后端和管理后台之间的地址与密钥必须按现场环境重新配置

完整步骤请参阅仓库根目录的 `README.md`、`Server/README.md`、`Client/README.md` 和 `vue/README.md`。
