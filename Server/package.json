{
  "name": "realname-auth-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "ncc build src/server.js -o dist"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mysql2": "^3.15.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
