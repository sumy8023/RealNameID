{
  "name": "realname-admin-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/*.test.mjs\"",
    "build": "ncc build src/server.js -o dist"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mysql2": "^3.15.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
